# Dotfiles managed Git aliases
[alias]
  co = checkout
  st = status
  br = branch
  lg = log --oneline --graph --decorate --all
  last = log -1 HEAD
  unstage = reset HEAD --
  amend = commit --amend
  undo = reset --soft HEAD~1
